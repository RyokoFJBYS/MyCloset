<h1>Posts</h1>

<% @posts.each do |post| %>
<div>
  <%= attachment_image_tag post_image, :image, :fill, 400, 300 %>
  <p>Title：<%= post.title %></p>
  <p>Date：<%= post.created_at.strftime('%Y/%m/%d') %></p>
  <p>User Name：<%= post.user.name %></p>
</div>
<% end %>
